/**
 * Weights below this fraction (0–1) are treated as negligible for portfolio geography,
 * sectors, charts, and related API logic. 0.01% = 0.0001.
 */
export const MIN_PORTFOLIO_ALLOCATION_FRACTION = 0.0001;
