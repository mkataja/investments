services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: investments
      POSTGRES_PASSWORD: investments
      POSTGRES_DB: investments
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data
